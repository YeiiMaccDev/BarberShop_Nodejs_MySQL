<div class="container p-4">
  <div class="row">
    <div class="col-md-4 mx-auto">
      <div class="card card-body bg-dark text-white text-center">
        <h2 class="fw-bold text-white">Perfil.</h2>
        <h5 class="fw-bold text-white">Bienvenido {{user.fullname}}</h5>
        <p class=" text-white">{{user.email}}</p>
        <p class=" text-white">{{timeago user.created_at}}</p>
        <a href="/reservations" class="btn btn-primary m-4">
          Guarde sus reservaciones.
        </a>
      </div>
    </div>

  </div>
</div>